# Architecture Overview

BluePhoenix uses a GitHub‑first architecture.

- Local endpoints generate telemetry
- Logs are exported and stored as JSON
- Detection logic is written as code
- Automation workflows are designed declaratively
- GitHub serves as the system of record

This mirrors real detection‑engineering workflows used by security teams.
